# spybot
## 📌 Що робить бот  

- Приймає від вас команду виду **`/run <shell‑команда>`** і **виконує її в терміналі** (повертає stdout / stderr).  
- Після натискання **кнопки «Зробити скріншот 📸»** (або після команди `/shot`) робить **знімок екрану** і відправляє його вам у Telegram.  

> **⚠️ Увага!** Виконувати довільні команди з Telegram – це **дуже небезпечно**. Нижче наведено мінімальні обмеження (білий список команд), але в продакшені рекомендується запускати бота в **ізольованому контейнері** або **віртуальній машині** під обмеженими правами.

---

## 1️⃣ Створення бота в BotFather  

1. Відкрийте `@BotFather` у Telegram.  
2. Відправте `/newbot`, дайте боту **ім’я** і **username** (`…_bot`).  
3. Отримайте **токен** – рядок типу `123456789:AAEhBO…`. Збережіть його, він знадобиться.

---

## 2️⃣ Необхідні залежності  

```bash
# Python 3.10+ (рекомендовано 3.11)
pip install -U pip

# Основні бібліотеки
pip install python-telegram-bot==20.7   # актуальна бібліотека v20
pip install mss pillow           # для скріншотів
```

> **Важливо:** `pyautogui` потребує активної X‑сесії (Linux) або графічного середовища (Windows/macOS). Якщо бот крутиться на сервері без GUI, використайте альтернативний інструмент (див. розділ «Альтернативний скріншот»).
